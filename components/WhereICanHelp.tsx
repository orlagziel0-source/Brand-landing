// This section was merged into WhatCanBeBuilt.tsx and is no longer used
// anywhere in the site. The file is kept here (instead of being deleted)
// only so that dragging this project's files over an older copy safely
// overwrites the old version of this file too — otherwise the old file
// stays behind and breaks the build, since it referenced dictionary
// fields that no longer exist.
//
// It is safe to delete this file entirely.
export default function WhereICanHelp() {
  return null;
}
